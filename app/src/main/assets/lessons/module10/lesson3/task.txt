Реализуй функцию player_move(board), которая спрашивает у игрока номер клетки (1-9), проверяет, свободна ли она, и если да — ставит 'X'. Если клетка занята или введено не число, выводи сообщение об ошибке и проси снова.